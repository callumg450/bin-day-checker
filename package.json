{
  "name": "bin-day-checker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "postinstall": "if [ \"$RUN_PLAYWRIGHT_INSTALL\" = \"true\" ]; then npx playwright install --with-deps; fi",
    "cron": "node cron/checkBinDay.js",
    "api": "node server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "playwright": "^1.54.0"
  },
  "devDependencies": {
    "prisma": "^6.11.1"
  }
}
